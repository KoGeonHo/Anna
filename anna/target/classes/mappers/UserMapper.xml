<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="edu.fourmen.mapper.userMapper">

	<select id="emailChk" parameterType="string" resultType="_int">
	
		SELECT 
			count(*) as cnt
		FROM 
			AnnaUser
		WHERE 
			user_email = #{ user_email } AND
			isDel = 'N'
			
	</select>
	
	<insert id="join" parameterType="UserVO">
	
		INSERT INTO AnnaUser(
			nickName,
			user_email,
			kakao_auth,
			profile_image,
			user_pwd
		) VALUES (
			#{ nickName },
			#{ user_email },
			#{ kakao_auth },
			#{ profile_image },
			#{ user_pwd }
		)
		
	</insert>
	
	<select id="login" parameterType="UserVO" resultType="UserVO">
	
		SELECT 
			uidx,
			nickName,
			user_email,
			kakao_auth,
			interested,
			user_pwd
		FROM 
			AnnaUser
		WHERE
			user_email = #{ user_email } AND
			isDel = 'N'
			
	</select>
	
	<update id="updateKakaoAuthKey" parameterType="UserVO">
	
		UPDATE 
			AnnaUser
		SET
			kakao_auth = #{ kakao_auth }
		WHERE
			uidx = #{ uidx }
	
	</update>
	
	<select id="getUsetInfo" parameterType="int" resultType="UserVO">
		
		SELECT
			nickName,
			user_email,
			addr1,
			addr2,
			addr_code,
			interested,
			location_auth,
			kakao_auth,
			profile_image,
			introduce,
			jdate
		FROM
			AnnaUser
		WHERE
			uidx = #{ uidx }
		
	</select>
	
</mapper>